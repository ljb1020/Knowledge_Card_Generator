import { TUTORIAL_SECTIONS } from '../services/tutorialDraft.js';

const TUTORIAL_STYLE_RULES = [
  '所有内容必须使用简体中文。',
  '目标不是知识摘要，而是为固定 4 个逻辑卡位的“面试作答卡组”提供可直接装配的底稿。',
  '必须严格围绕用户给定的那个知识点展开，不能偷换成相关概念、上位概念或相邻概念。',
  '标题必须出现且仅出现一次，顺序固定，标题文字完全一致。',
  '不要输出 JSON、HTML 或 Markdown 代码块。但你可以使用 **加粗** 语法来高亮核心得分点或专业名词。',
  '整体要有面试训练价值，不要写成百科词条。',
];

const TUTORIAL_SECTION_RULES = [
  '“一句话定义”用 1 到 2 句讲清这个知识点是什么，第一句必须直接点名该知识点。',
  '“它解决什么问题”必须落到前端工程里的真实价值、真实痛点或为什么会被问。',
  '“标准面试回答”写成一段可直接对面试官复述的中文回答，长度目标约 300 到 600 字，可以稍微口语化，但必须完整。',
  '“回答里的关键论据”用于拆出支撑回答的抓手，至少覆盖机制、代码、场景中的 1 类，最好写成 3 到 5 条。',
  '“面试官最可能追问的 3-5 个问题”必须按 1. 2. 3. 的编号强制分行，写满 3 到 5 个高频追问。必须写成“问题？——深入的答题方向和原理解释点拨”的格式。单条必须在 60 到 100 字，必须有血有肉，不能是干瘪的提纲，要在解答里直接塞满可以直接复述的专业话术。',
  '“这道题的易错点”必须按 1. 2. 3. 的编号强制分行，写满 3 到 5 个易错点。必须结合真实的工程场景或常见的概念混淆点，写出“具体的易错场景 + 为什么这么做会错 / 深入的避坑指南”。单条必须在 60 到 100 字。',
];

function buildTutorialRulesBlock(): string[] {
  return [
    '固定标题：',
    ...TUTORIAL_SECTIONS.map((section) => section.title),
    '',
    '通用规则：',
    ...TUTORIAL_STYLE_RULES.map((rule) => `- ${rule}`),
    '',
    '分标题要求：',
    ...TUTORIAL_SECTION_RULES.map((rule) => `- ${rule}`),
  ];
}

function buildTopicLockRules(topic: string): string[] {
  return [
    '知识点锁定规则：',
    `- 本次唯一知识点是“${topic}”，不能替换成别的概念。`,
    `- “一句话定义”第一句必须直接写成“${topic}是……”或“${topic}（……）是……”。`,
    '- 如果内容主体写成别的概念，整份底稿视为失败。',
  ];
}

const ONE_SHOT_EXAMPLE = `
=== 优秀参考样例（目标知识点：闭包） ===
一句话定义：
闭包（Closure）是指一个函数以及该函数被声明时所处的词法环境的组合。

它解决什么问题：
在前端工程中，闭包主要解决了**状态的持久化保存**和**变量的私有化隔离**问题，比如防抖节流函数中的定时器状态、高阶函数的数据缓存、以及 React Hook 中跨渲染周期的状态捕获，都是闭包的真实应用。

标准面试回答：
面试官您好，我认为闭包本质上就是**函数与它被创建时所在的词法环境的结合**。
它不是什么特殊的语法糖，而是由词法作用域规则决定的副产物。它的核心价值在于，**即使外层函数已经执行结束并从调用栈中弹出，内层函数依然能持有对外部变量的引用，导致这些变量留在内存中不被销毁**。
在实际开发中，我们天天都在用闭包。比如**防抖和节流**里的 timer 变量，比如早期立即执行函数（IIFE）构建的模块化私有变量，再比如现在非常高频的 **React Hooks**。每次组件 render 时都会生成新的闭包来捕获当时的 props 和 state。
但面试时我们也常讨论它的副作用：如果不加注意地在事件回调或是定时器里引入闭包，且没有在组件卸载时清理，确实会引发**内存泄漏**。因此，闭包是一把很强的双刃剑，关键在于生命周期管理。

回答里的关键论据：
1. 机制：内层函数保持对外部词法环境的引用。
2. 机制：外层函数弹出调用栈后，变量仍不被辣鸡回收处理收集。
3. 场景：防抖、节流、局部作用域封装、React Hooks。
4. 场景：不当引用导致内存泄漏的潜在风险。

面试官最可能追问的 3-5 个问题：
1. 为什么外层函数执行结束后，变量还不会消失？——因为 JavaScript 引擎有垃圾回收机制，但只要该环境下的变量还在被更深层的内部函数引用，这层词法环境就无法被标记清除。闭包正是通过这种强引用，将变量保存在堆内存中。
2. React 的 useEffect 经常遇到陈旧的闭包陷阱（Stale Closure），这是为什么？——因为组件每次渲染都是一个独立的闭包环境，如果 useEffect 的依赖数组漏写了某个 state，它内部使用的永远是它被**初始定义那次渲染时捕获的旧 state 值**，这种本质正是闭包的词法作用域特性决定的。
3. 闭包和作用域链是什么关系？——闭包的存在**依赖于**作用域链，可以说闭包就是作用域链在函数嵌套场景下的一种必然展现。当内部函数试图访问一个变量时，它会沿着作用域链向外查找，这根看不见的“链条”把内外部牢牢绑在一起。

这道题的易错点：
1. **误区一：只要有嵌套函数就是闭包。** 很多人以为只要写了函数嵌套就是闭包，其实不是。关键看内部函数是否**实际引用**了外部变量，并且该内部函数在**外部作用域被执行或持久保存**。如果没有实际引用，现代 JS 引擎优化会直接回收它。
2. **误区二：闭包=内存泄漏。** 这是一个经典误区。闭包只是正常占用必要的内存来保存状态。真正的内存泄漏是由于开发者代码逻辑问题（如给 window 或长生命周期的 DOM 节点绑定了无用闭包）导致**这些不再使用的引用一直没有被打断**。
3. **误区三：搞混闭包与 this 绑定。** 闭包与词法作用域相关（取决于函数**写在哪里**），而 this 是动态绑定的（取决于函数**如何被调用**）。箭头函数没有自己的 this，而是通过闭包去捕获外层的 this，这两者常常在面试题中结合出现来迷惑候选人。
=================================================
`;

export function buildStage1Prompt(topic: string): string {
  return [
    '你是一名前端面试教练。',
    '请围绕给定知识点，直接输出一份“面试作答底稿”。',
    '这份底稿后续会被提炼成 4 个逻辑卡位：cover、标准回答、追问、易错点。',
    '不要提问，不要索取更多上下文，不要解释你要做什么。',
    '',
    ...buildTopicLockRules(topic),
    '',
    ...buildTutorialRulesBlock(),
    '',
    `唯一知识点：${topic}`,
    '',
    ONE_SHOT_EXAMPLE,
  ].join('\n');
}

export function buildStage1RetryPrompt(
  topic: string,
  previousDraft: string,
  validationError: string
): string {
  return [
    '你上一版输出未通过校验，现在必须完整重写一份合格的“面试作答底稿”。',
    '这不是局部修补，必须重新输出最终版本，并继续遵守全部结构规则、长度规则和内容覆盖规则。',
    '尤其注意：追问和易错点都必须按编号单独分行写，不要挤成一大段。',
    '如果追问或易错点超长，请优先压缩到“一行一个点，每行一句主干 + 一句点拨”的写法。',
    '',
    ...buildTopicLockRules(topic),
    '',
    ...buildTutorialRulesBlock(),
    '',
    `唯一知识点：${topic}`,
    '',
    ONE_SHOT_EXAMPLE,
    '',
    '校验错误：',
    validationError,
    '',
    '上一版底稿：',
    previousDraft,
  ].join('\n');
}
